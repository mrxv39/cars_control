node_modules/
dist/
dist-web/
build/
target/
*.db
*.pyc
__pycache__/
.env
.env.local
.env.*
.vite/
app-dev.log

# Credenciales criptográficas — nunca commitear
*.pem
*.key
*.cert
*.p12
*.pfx

# Datos sensibles
data/
backups/
app/src-tauri/data/clients.json
app/src-tauri/data/leads.json

# Archivos temporales de scraping
gastos_extracted.json
gastos_parsed.json
gastos_ocr_parsed.json
coches_page.html
coches_text.txt
coches_screenshot.png
coches_blocked.png
coches_loaded.png
extracted_invoices.json
coches_stock.json
docs_legacy/
.claude/
*.log
.coverage

# Datos bancarios sensibles — NUNCA commitear PDFs de extractos
tmp_caixa_eml/
extractos/
*.n43
cuenta_*.pdf
cuenta_*.xls
cuenta_*.xlsx

# Vercel CLI — project link local, no commitear
.vercel/

# Python — entornos virtuales y artefactos de tests
.venv/
venv/
.pytest_cache/
htmlcov/

# TypeScript — build incremental y coverage JS
*.tsbuildinfo
coverage/

# Supabase CLI — estado local y ramas de desarrollo
.supabase/
supabase/.branches/

# Playwright — auth state y resultados
app/e2e/.auth/
app/playwright-report/
app/test-results/
